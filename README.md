# 🎓 ВУЗресурс - Навигационная платформа для поступления в ВУЗ

Простой и эффективный веб-сайт, помогающий школьникам 8-11 классов подобрать олимпиады, курсы, лицеи, летние программы и другие ресурсы для поступления в лучшие московские ВУЗы.

## 🚀 Быстрый старт

### Способ 1: С локальным сервером Python (Рекомендуется)

1. Распакуйте архив в папку
2. Откройте терминал/командную строку в этой папке
3. Запустите сервер:

```bash
python -m http.server 8000
```

1. Откройте в браузере: `http://localhost:8000`
2. Готово! Сайт работает

### Способ 2: Локально (Без сервера)

1. Распакуйте архив в папку
2. Откройте `index.html` в браузере (двойной клик)
3. **⚠️ Может не работать - используйте Способ 1**

### Способ 3: На веб-хостинге (FTP)

1. Загрузите все файлы на хостинг:
   - `index.html`, `styles.css`
   - папка `js/` со всеми JS файлами
   - папка `data/` со всеми JSON файлами
2. Откройте `index.html` через браузер
3. Готово!

### Способ 4: GitHub Pages (Бесплатно)

1. Создайте репозиторий на GitHub
2. Загрузите все файлы в репозиторий
3. Settings → Pages → выберите branch → Save
4. Сайт доступен по адресу: `https://sto200.github.io/vuz_resurs`

## 📁 Структура файлов

```
project/
├── index.html              # Главная страница
├── styles.css              # Стили и дизайн
├── js/
│   ├── app.js              # Инициализация
│   ├── state.js            # Управление состоянием
│   ├── data-loader.js      # Загрузка данных
│   ├── filters.js          # Логика фильтрации
│   ├── ui-renderer.js      # Рендеринг UI
│   ├── resource-cards.js   # Создание карточек
│   └── event-handlers.js   # Обработчики событий
└── data/
    └── universities/
        └── hse/            # Данные для каждого ВУЗа
            ├── olympiads.json
            ├── courses.json
            ├── schools.json
            ├── summerPrograms.json
            ├── practicalEvents.json
            ├── infoEvents.json
            ├── educationalEvents.json
            └── onlineResources.json
```

## ✨ Функции

- ✅ **Выбор ВУЗа** - Подберите ресурсы для ВШЭ, Финуни, РЭУ и МГУ
- ✅ **8 категорий ресурсов** - Олимпиады, курсы, лицеи, летние программы, практические и образовательные события, онлайн-ресурсы
- ✅ **Умная фильтрация** - По типу, стоимости, льготам
- ✅ **Адаптивный дизайн** - Работает везде (ПК, планшет, мобильный)
- ✅ **Полная информация** - Даты, стоимость, льготы, ссылки
- ✅ **Zero-зависимостей** - Чистый HTML/CSS/JS

## 🎯 Поддерживаемые ВУЗы

1. **ВШЭ** - Экономика, бизнес, менеджмент
2. **Финуни** - Финансы, экономика, банковское дело
3. **РЭУ** - Классический экономический университет
4. **МГУ** - Все направления (в разработке)

## 📊 Ресурсы в базе

- 🏆 **Олимпиады** - С информацией о льготах и уровнях
- 📚 **Курсы** - Онлайн и очные программы подготовки
- 🎓 **Лицеи** - Профильные классы при ВУЗах
- ☀️ **Летние программы** - Интенсивные программы
- 💡 **Практические события** - Хакатоны, кейс-чемпионаты
- ℹ️ **Информационные события** - Дни открытых дверей, встречи
- 👨‍🏫 **Образовательные события** - Лекции, мастер-классы
- 🌐 **Онлайн-ресурсы** - YouTube, Telegram, порталы

## 🔧 Редактирование данных

Данные находятся в папке `data/universities/[вуз]/` в формате JSON.

Чтобы добавить новый ресурс, отредактируйте соответствующий `.json` файл:

```json
{
    "id": "уникальный_id",
    "name": "Название",
    "description": "Описание",
    "type": "online",
    "format": "онлайн",
    "targetAudience": ["10 класс", "11 класс"],
    "subjects": ["Математика", "Физика"],
    "cost": {
        "type": "платно",
        "amount": 25000,
        "currency": "RUB"
    },
    "dates": {
        "registration": "01.09 - 30.11.2026"
    },
    "website": "https://example.com",
    "benefits": {
        "bvi": false,
        "points100": false,
        "additionalPoints": 0,
        "grants": null,
        "tuitionDiscount": null,
        "priority": false,
        "earlyAdmission": false
    }
}
```

## ✅ Валидация данных

### Что это?

**MCP-сервис для валидации ресурсов** — инструмент для автоматической проверки и исправления описаний образовательных ресурсов с помощью Claude Code. Сервис сравнивает текущие описания с контентом веб-сайтов и предлагает улучшения.

### Как запустить?

#### 1. Перезагрузи Claude Code

```bash
exit
claude
```

#### 2. Используй инструменты в Claude Code

Просто пиши команды, и MCP сервис будет их выполнять:

```
Прочитай все ресурсы из universities/mpei/infoEvents.json
```

### Полный рабочий процесс: Валидация → Исправление → Отчет

#### Шаг 1: Загрузить ресурсы

```
Прочитай ресурсы из universities/mpei/infoEvents.json
```

#### Шаг 2: Захватить контент веб-сайта

```
fetch_webpage("https://abitur-mpei.ru/open-day")
```

#### Шаг 3: Валидировать описания

```
Для каждого ресурса:
1. Валидируй описание используя validate_resource
2. Если confidence < 75%, предложи улучшение
```

#### Шаг 4: Обновить описания

```
Если статус NEEDS_UPDATE или MISMATCH:
- Используй suggested_description из результата валидации
- Обнови описание в ресурсе
```

#### Шаг 5: Сохранить изменения

```
update_json_file("universities/mpei/infoEvents.json", json.dumps(updated_resources))
```

#### Шаг 6: Сгенерировать отчет

```
save_validation_report(validation_results, "validation_report.csv")
```

### Пример команды для быстрого старта

```
Давай исправим все описания в universities/mpei/infoEvents.json:

1. Прочитай все ресурсы
2. Для каждого (возьми первые 5):
   - Захвати его веб-сайт
   - Проверь описание (validate_resource)
   - Если статус не OK, обнови описание используя suggested_description
3. Сохрани обновленные ресурсы обратно в JSON
4. Создай отчет о всех проделанных изменениях в CSV
```

### Доступные инструменты (Tools)

| Инструмент | Описание |
|-----------|---------|
| `read_json_file(path)` | Читает JSON файл с ресурсами |
| `fetch_webpage(url)` | Захватывает контент веб-страницы |
| `validate_resource(id, description, content)` | Проверяет описание ресурса |
| `batch_get_resources(path, start, count)` | Получает пакет ресурсов |
| `update_json_file(path, data)` | Сохраняет исправленные ресурсы |
| `save_validation_report(data, filename)` | Генерирует отчет (CSV/JSON/HTML) |
| `list_resources(path)` | Список всех ресурсов |
| `get_resource_by_id(path, id)` | Поиск по ID |

### Метрики валидации

Каждое описание оценивается по трем критериям:

- 🔑 **Ключевые слова (40%)** — совпадение важных терминов
- 📝 **Сходство текста (40%)** — строковое сравнение
- 📏 **Длина описания (20%)** — адекватность объема

**Результаты:**

- 🟢 **OK** (> 75%) — описание хорошее, не требует изменений
- 🟡 **NEEDS_UPDATE** (50-75%) — требует уточнения
- 🔴 **MISMATCH** (< 50%) — нужно переписать

### Результаты валидации

Отчеты генерируются в 3 форматах:

- 📊 **CSV** — для анализа в Excel (`validation_report.csv`)
- 📄 **JSON** — структурированные данные (`validation_report.json`)
- 🌐 **HTML** — красивый интерактивный вид (`validation_report.html`)

Все отчеты сохраняются в папку `/reports/`

### Резервные копии

При каждом обновлении JSON файла автоматически создается резервная копия:

```
universities/mpei/infoEvents.json.backup
```

Это позволяет легко вернуться к предыдущей версии, если что-то пошло не так.

### Подробная документация

Для полной документации MCP сервиса смотри: [`mcp/README.md`](./mcp/README.md)

Для информации о релизе смотри: [`RELEASE_NOTES.md`](./RELEASE_NOTES.md)

## 🎨 Дизайн

Изменить цвета в `styles.css`:

```css
:root {
    --primary: #2160A0;        /* Основной синий */
    --secondary: #06B6D4;      /* Голубой */
    --accent: #F59E0B;         /* Жёлтый */
}
```

## 📱 Адаптивность

- **Desktop** (1200px+) - 3-4 колонки
- **Tablet** (768px-1199px) - 2 колонки
- **Mobile** (до 768px) - 1 колонка

## 🌐 Поддержка браузеров

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+
- ✅ Opera 76+

## 🐛 Решение проблем

### Ошибка: "Cannot GET /"

**Решение:** Используйте Python HTTP сервер (Способ 1)

```bash
python -m http.server 8000
```

### Данные не загружаются

**Проверьте:**

- Структура папок соответствует `data/universities/[вуз]/*.json`
- Запущен локальный сервер (не просто открыт файл в браузере)
- JSON файлы валидные (без синтаксических ошибок)

### Модули не загружаются

**Причина:** Открыли `index.html` как файл, а не через сервер
**Решение:** Используйте `python -m http.server 8000`

## 📚 Документация

- [📋 Модель данных](./docs/модель_данных.md) - Полная спецификация структуры данных V2
- [🔍 Промпт для поиска](./docs/промпт_для_поиска.md) - Системный промпт для работы с проектом
- [🏗️ Структура модулей](./docs/structure.html) - Интерактивная схема архитектуры

## 👨‍💻 Разработано

**Стефан Огульник** 2026

## 📄 Лицензия

Свободен для использования в образовательных целях.

---

**Помогаем школьникам поступить в лучшие ВУЗы!** 🎓
