# 🚀 MCP-сервис для валидации ресурсов ВУЗов

Полностью автоматизированный MCP (Model Context Protocol) сервис для проверки и валидации 800+ карточек образовательных ресурсов российских ВУЗов.

## ✨ Возможности

- ✅ **Чтение JSON файлов** с ресурсами
- ✅ **Захват веб-страниц** и извлечение контента
- ✅ **Валидация описаний** по контенту сайта
- ✅ **Пакетная обработка** множества ресурсов
- ✅ **Обновление JSON** с исправленными описаниями
- ✅ **Генерация отчетов** (CSV, JSON, HTML)
- ✅ **Работает в Claude Code** (WSL, Linux, macOS)

## 📦 Требования

- Python 3.10+
- WSL 2 (для Windows) или Linux/macOS
- Claude Code или Claude Desktop

## 🔧 Установка

### 1. Клонировать репозиторий

```bash
mkdir -p ~/projects/resource-validator-mcp
cd ~/projects/resource-validator-mcp
```

### 2. Создать структуру проекта

```bash
# Скопировать все файлы в директорию
# mcp_server.py
# utils/
#   ├── web_scraper.py
#   ├── validator.py
#   ├── json_handler.py
#   └── report_generator.py
# requirements.txt
# data/
#   └── (здесь будут JSON файлы)
```

### 3. Установить зависимости

```bash
# Создать виртуальное окружение (рекомендуется)
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

### 4. Конфигурировать Claude Code

Отредактировать `~/.config/claude/config.json`:

```json
{
  "mcpServers": {
    "resource-validator": {
      "command": "python3",
      "args": [
        "/home/username/projects/resource-validator-mcp/mcp_server.py"
      ],
      "env": {
        "PYTHONPATH": "/home/username/projects/resource-validator-mcp"
      }
    }
  }
}
```

**Замени `/home/username` на свой путь!**

Для поиска пути:

```bash
echo $HOME
# Результат скопировать и вставить
```

### 5. Перезагрузить Claude Code

```bash
# Выход из Claude Code
# Ctrl+D или /exit

# Запуск заново
claude
```

## 📖 Использование

### Пример 1: Проверка одного ресурса

В Claude Code:

```
Проверь ресурс general_open_day_hse из файла hse/infoEvents.json
```

Claude автоматически использует MCP инструменты и вернет:

```
✅ Описание совпадает на 85%
Статус: OK
Ресурс актуален
```

### Пример 2: Пакетная валидация файла

```
Проверь все ресурсы в файле hse/infoEvents.json
```

Результат:

```
Проверено 7 ресурсов:
✅ 5 ресурсов OK
⚠️ 2 требуют обновления

Обновить описания?
```

### Пример 3: Полный цикл валидации и обновления

```
1. Прочитай все ресурсы из hse/infoEvents.json
2. Проверь каждый (первые 5)
3. Сохрани результаты в отчет
4. Обнови описания которые требуют обновления
```

## 🎛️ Доступные инструменты

### `read_json_file(filepath)`

Читает JSON файл с ресурсами

**Параметры:**

- `filepath` (string): Путь к файлу, например `hse/infoEvents.json`

**Пример:**

```
read_json_file("hse/infoEvents.json")
```

### `fetch_webpage(url, max_chars=3000)`

Захватывает содержимое веб-страницы

**Параметры:**

- `url` (string): URL страницы
- `max_chars` (int, optional): Максимум символов (default: 3000)

**Пример:**

```
fetch_webpage("https://dod.hse.ru", 5000)
```

### `validate_resource(resource_id, current_description, webpage_content)`

Валидирует описание ресурса

**Параметры:**

- `resource_id` (string): ID ресурса
- `current_description` (string): Текущее описание
- `webpage_content` (string): Содержимое сайта

**Возвращает:**

```json
{
  "validation_status": "OK" | "NEEDS_UPDATE" | "MISMATCH",
  "confidence": 0.85,
  "suggested_description": "...",
  "reasoning": "..."
}
```

### `batch_get_resources(filepath, start_index=0, count=10)`

Получает пакет ресурсов

**Параметры:**

- `filepath` (string): Путь к файлу
- `start_index` (int): Начальный индекс
- `count` (int): Количество ресурсов

**Пример:**

```
batch_get_resources("hse/infoEvents.json", 0, 5)
```

### `update_json_file(filepath, updated_data)`

Обновляет JSON файл

**Параметры:**

- `filepath` (string): Путь к файлу
- `updated_data` (string): JSON-строка с данными

**Пример:**

```
update_json_file("hse/infoEvents.json", "[{...}]")
```

### `save_validation_report(report_data, filename=None)`

Сохраняет отчет о валидации

**Параметры:**

- `report_data` (string): JSON-строка с результатами
- `filename` (string, optional): Имя файла

**Пример:**

```
save_validation_report("[{...}]", "report.csv")
```

### `list_resources(filepath)`

Выводит список всех ресурсов в файле

**Пример:**

```
list_resources("hse/infoEvents.json")
```

### `get_resource_by_id(filepath, resource_id)`

Получает полную информацию о ресурсе по ID

**Пример:**

```
get_resource_by_id("hse/infoEvents.json", "general_open_day_hse")
```

## 📊 Статусы валидации

- **OK** (зеленый) — Описание совпадает с контентом (> 75% совпадения)
- **NEEDS_UPDATE** (оранжевый) — Требует уточнения (50-75% совпадения)
- **MISMATCH** (красный) — Не соответствует контенту (< 50% совпадения)

## 📈 Метрики оценки

Валидация использует три метрики:

1. **Ключевые слова** (40%) — Совпадение важных слов
2. **Сходство** (40%) — Строковое сходство описания и контента
3. **Длина** (20%) — Адекватность длины описания

**Итоговая оценка:**

```
Score = Keywords × 0.4 + Similarity × 0.4 + Length × 0.2
```

## 📁 Структура проекта

```
resource-validator-mcp/
├── mcp_server.py              # Основной файл MCP
├── requirements.txt           # Зависимости
├── README.md                  # Этот файл
├── utils/
<!-- │   ├── __init__.py -->
│   ├── web_scraper.py         # Захват веб-страниц
│   ├── validator.py           # Валидация описаний
│   ├── json_handler.py        # Работа с JSON
│   └── report_generator.py    # Генерация отчетов
├── data/                      # JSON файлы с ресурсами
│   └── hse/
│       ├── infoEvents.json
│       ├── olympiads.json
│       └── ...
└── reports/                   # Сгенерированные отчеты
    ├── validation_report_20260122_021800.csv
    ├── validation_report_20260122_021800.json
    └── ...
```

## 🔍 Отчеты

Сервис генерирует отчеты в трех форматах:

### CSV Отчет

```csv
resource_id,validation_status,confidence,keywords_score,similarity_score,length_score,reasoning
general_open_day_hse,OK,0.85,0.8,0.9,0.75,Описание совпадает хорошо...
```

### JSON Отчет

```json
{
  "timestamp": "2026-01-22T02:18:00",
  "total_records": 7,
  "summary": {
    "ok": 5,
    "needs_update": 2,
    "mismatch": 0
  },
  "records": [...]
}
```

### HTML Отчет

Красивый интерактивный отчет с таблицей и статистикой

## ⚙️ Конфигурация

Отредактировать `mcp_server.py` для изменения настроек:

```python
# Директория с данными
DATA_DIR = "data"

# Директория с отчетами
REPORTS_DIR = "reports"

# Таймаут при загрузке страниц (секунды)
SCRAPER_TIMEOUT = 10

# Максимум попыток при ошибке
MAX_RETRIES = 3
```

## 🐛 Отладка

### Проверить подключение MCP

```bash
# В Claude Code
/claude mcp list
```

Должны увидеть:

```
✅ resource-validator
   - read_json_file
   - fetch_webpage
   - validate_resource
   - batch_get_resources
   - update_json_file
   - save_validation_report
   - list_resources
   - get_resource_by_id
```

### Посмотреть логи

```bash
# Логи сохраняются в stdout при запуске
python3 mcp_server.py
```

### Проверить JSON файл

```bash
# Валидировать JSON синтаксис
python3 -m json.tool data/hse/infoEvents.json | head -20
```

## 🚨 Решение проблем

### Ошибка: "mcp not found"

```bash
pip install mcp fastmcp
```

### Ошибка: "Файл не найден"

Убедиться что файл находится в директории `data/`:

```bash
ls -la data/hse/
# Должен вывести:
# -rw-r--r--  infoEvents.json
```

### Ошибка: "Cannot connect to server"

1. Перезагрузить Claude Code
2. Проверить путь в `config.json` (использовать полный путь)
3. Убедиться что Python скрипт исполняемый:

   ```bash
   chmod +x mcp_server.py
   ```

### Таймаут при загрузке страницы

Некоторые сайты могут требовать больше времени. Увеличить таймаут:

```python
# В mcp_server.py
scraper = WebScraper(timeout=20)  # 20 секунд вместо 10
```

## 📊 Примеры использования

### Валидация одного файла

```
"Проверь все ресурсы в data/hse/infoEvents.json.
Для каждого захвати контент сайта, проверь совпадение и сохрани отчет."
```

### Валидация нескольких ВУЗов

```
"Проверь ресурсы из:
- data/hse/infoEvents.json (первые 10)
- data/finuni/infoEvents.json (первые 10)
- data/msu/infoEvents.json (первые 10)

Объедини результаты в один отчет."
```

### Обновление описаний

```
"Найди все ресурсы со статусом NEEDS_UPDATE.
Обнови их описания согласно предложениям.
Сохрани обновленный JSON файл."
```

## 📝 Лицензия

MIT License

## 🤝 Поддержка

Если возникают проблемы, проверьте:

1. Установлены ли все зависимости: `pip list`
2. Правильный ли путь в config.json: `echo $HOME`
3. Работает ли Python: `python3 --version`
4. Подключен ли MCP: `/claude mcp list`

---

**Готово! Начните валидировать ресурсы прямо в Claude Code!** 🚀
