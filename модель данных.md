# 📋 Модель данных data.json - Полное руководство

## 🏗️ Общая структура

Файл `data.json` содержит всё для работы сайта и состоит из **6 основных разделов**:

```json
{
  "universities": [...],      // ВУЗы
  "olympiads": [...],         // Олимпиады
  "onlineCourses": [...],     // Онлайн-курсы
  "offlineCourses": [...],    // Очные курсы
  "profileClasses": [...],    // Профильные классы
  "festivals": [...]          // Фестивали
}
```

***

## 🎓 1. Universities (ВУЗы)

Основной справочник университетов. ID используются в других разделах для связи.

### Структура объекта

```json
{
  "id": "string",              // Уникальный идентификатор (используется в связях)
  "name": "string",            // Полное название ВУЗа
  "description": "string"      // Краткое описание (1 строка)
}
```

### Пример добавления нового ВУЗа

```json
{
  "id": "ranepa",
  "name": "РАНХиГС (Российская академия народного хозяйства)",
  "description": "Ведущая бизнес-школа России при Президенте РФ"
}
```

### ⚠️ Важно
- `id` должен быть **уникальным** (латиница, без пробелов)
- Используйте краткие названия: `msu`, `mgimo`, `ranepa`
- После добавления ВУЗа, добавьте его `id` в поле `relevantUniversities` нужных ресурсов

***

## 🏆 2. Olympiads (Олимпиады)

Информация о школьных олимпиадах с льготами при поступлении.

### Структура объекта

```json
{
  "id": "string",                    // Уникальный ID
  "name": "string",                  // Название олимпиады
  "level": "I" | "II" | "III",       // Уровень РСОШ (I, II или III)
  "subjects": ["string"],            // Массив предметов
  "benefits": {                       // Льготы при поступлении
    "bvi": boolean,                   // БВИ (поступление без экзаменов)
    "points100": boolean,             // 100 баллов за ЕГЭ
    "additionalPoints": number        // Дополнительные баллы (0-10)
  },
  "website": "string",               // URL официального сайта
  "relevantUniversities": ["string"], // Массив ID ВУЗов
  "description": "string"            // Краткое описание
}
```

### Пример добавления олимпиады

```json
{
  "id": "mgimo_olympiad",
  "name": "Покори Воробьёвы горы!",
  "level": "I",
  "subjects": ["История", "Обществознание", "Иностранные языки"],
  "benefits": {
    "bvi": true,
    "points100": true,
    "additionalPoints": 0
  },
  "website": "https://pvg.mk.ru",
  "relevantUniversities": ["hse", "mgimo"],
  "description": "Олимпиада I уровня от МГУ и МГИМО с максимальными льготами"
}
```

### 📌 Поля для заполнения

- **level**: `"I"` (лучший), `"II"` или `"III"` – уровень в Перечне РСОШ
- **subjects**: Список предметов через запятую в массиве
- **benefits.bvi**: `true` если даёт поступление без вступительных испытаний
- **benefits.points100**: `true` если даёт 100 баллов за ЕГЭ по профильному предмету
- **benefits.additionalPoints**: Число от 0 до 10 (дополнительные баллы к ЕГЭ)

***

## 📱 3. OnlineCourses (Онлайн-курсы)

Дистанционные образовательные программы.

### Структура объекта

```json
{
  "id": "string",                    // Уникальный ID
  "name": "string",                  // Название курса/платформы
  "platform": "string",              // Организатор курса
  "isFree": boolean,                 // Бесплатный (true) или платный (false)
  "website": "string",               // URL курса
  "relevantUniversities": ["string"], // Массив ID ВУЗов
  "description": "string"            // Краткое описание
}
```

### Пример

```json
{
  "id": "msu_online",
  "name": "МГУ Онлайн",
  "platform": "МГУ им. Ломоносова",
  "isFree": true,
  "website": "https://online.msu.ru",
  "relevantUniversities": ["msu"],
  "description": "Бесплатные онлайн-курсы от МГУ по всем направлениям"
}
```

### 📌 Поля

- **isFree**: `true` = бесплатный, `false` = платный
- **platform**: Название организации/платформы (ВШЭ, Coursera, Stepik и т.д.)

***

## 📚 4. OfflineCourses (Очные курсы)

Подготовительные курсы и занятия на территории ВУЗа.

### Структура объекта

```json
{
  "id": "string",                    // Уникальный ID
  "name": "string",                  // Название программы
  "organizer": "string",             // Организатор
  "price": number,                   // Стоимость (0 = уточняется)
  "website": "string",               // URL
  "relevantUniversities": ["string"], // Массив ID ВУЗов
  "description": "string"            // Описание
}
```

### Пример

```json
{
  "id": "mgimo_prep",
  "name": "Подготовительные курсы МГИМО",
  "organizer": "МГИМО МИД России",
  "price": 85000,
  "website": "https://mgimo.ru/study/courses/",
  "relevantUniversities": ["mgimo"],
  "description": "Годовые курсы подготовки к ЕГЭ по всем предметам"
}
```

### 📌 Поля

- **price**: Указывайте в рублях. Если цена неизвестна – ставьте `0`
- **organizer**: Полное название организатора

***

## 🎓 5. ProfileClasses (Профильные классы)

Специализированные школьные классы при университетах.

### Структура объекта

```json
{
  "id": "string",                    // Уникальный ID
  "schoolName": "string",            // Название школы/лицея
  "profile": "string",               // Профили обучения
  "contacts": "string",              // Контактная информация
  "website": "string",               // URL (опционально)
  "relevantUniversities": ["string"] // Массив ID ВУЗов
}
```

### Пример

```json
{
  "id": "msu_school",
  "schoolName": "Школа-интернат МГУ им. А.Н. Колмогорова",
  "profile": "Физико-математический, Химико-биологический",
  "contacts": "10-11 классы, пансион при МГУ",
  "website": "https://internat.msu.ru",
  "relevantUniversities": ["msu"]
}
```

### 📌 Поля

- **profile**: Перечислите все профили через запятую
- **contacts**: Классы, форма обучения, особенности поступления

***

## 🎉 6. Festivals (Фестивали)

Научные мероприятия, дни открытых дверей, лекции.

### Структура объекта

```json
{
  "id": "string",                    // Уникальный ID
  "name": "string",                  // Название мероприятия
  "date": "YYYY-MM-DD",              // Дата в формате ISO 8601
  "website": "string",               // URL для регистрации
  "relevantUniversities": ["string"], // Массив ID ВУЗов
  "description": "string"            // Описание
}
```

### Пример

```json
{
  "id": "msu_day",
  "name": "День открытых дверей МГУ",
  "date": "2026-03-15",
  "website": "https://msu.ru/entrance/openday/",
  "relevantUniversities": ["msu"],
  "description": "Март: знакомство с факультетами, встречи с деканами, экскурсии"
}
```

### 📌 Поля

- **date**: Формат `"YYYY-MM-DD"` (например, `"2026-09-20"`)
- Если точная дата неизвестна – укажите первый день месяца

***

## 🔗 Связь между разделами

### Ключевое поле: `relevantUniversities`

Это массив ID университетов, для которых подходит ресурс:

```json
"relevantUniversities": ["hse", "finuni", "plekhanov"]
```

### Правила:
- ✅ **Один ВУЗ**: `["hse"]`
- ✅ **Несколько ВУЗов**: `["hse", "msu", "mgimo"]`
- ✅ **Все ВУЗы**: `["hse", "finuni", "plekhanov"]`
- ❌ **Пустой массив недопустим**: минимум 1 ID

***

## ✨ Пошаговая инструкция добавления нового ВУЗа

### Шаг 1: Добавьте ВУЗ в раздел `universities`

```json
{
  "id": "msu",
  "name": "МГУ им. М.В. Ломоносова",
  "description": "Старейший и крупнейший классический университет России"
}
```

### Шаг 2: Добавьте ресурсы для этого ВУЗа

Например, олимпиаду:
```json
{
  "id": "lomonosov_olympiad",
  "name": "Олимпиада школьников «Ломоносов»",
  "level": "I",
  "subjects": ["Математика", "Физика", "Химия"],
  "benefits": {"bvi": true, "points100": true, "additionalPoints": 0},
  "website": "https://olymp.msu.ru",
  "relevantUniversities": ["msu"],
  "description": "Олимпиада I уровня, 20+ предметов"
}
```

### Шаг 3: Добавьте онлайн-курсы, очные курсы, классы, фестивали

Используйте структуры из примеров выше, указывая `"relevantUniversities": ["msu"]`

***

## 🛡️ Валидация данных

### Обязательные поля (нельзя пропустить):

**Universities:**
- `id`, `name`, `description`

**Olympiads:**
- `id`, `name`, `level`, `subjects`, `benefits`, `website`, `relevantUniversities`

**Courses (online/offline):**
- `id`, `name`, `website`, `relevantUniversities`

**ProfileClasses:**
- `id`, `schoolName`, `profile`, `contacts`, `relevantUniversities`

**Festivals:**
- `id`, `name`, `date`, `website`, `relevantUniversities`

### Проверка JSON:
После редактирования проверьте файл на https://jsonlint.com

***

## 💡 Советы по заполнению

1. **ID всегда латиницей** без пробелов: `msu_olympiad`, `mgimo_courses`
2. **Даты в формате ISO**: `2026-09-15` (год-месяц-день)
3. **URL полностью**: `https://example.com` (с протоколом)
4. **Boolean значения** без кавычек: `true`, `false` (не `"true"`)
5. **Числа без кавычек**: `100`, `85000` (не `"100"`)
6. **Массивы в квадратных скобках**: `["item1", "item2"]`

***

## 📊 Быстрый чеклист добавления ВУЗа

- [ ] Добавил ВУЗ в `universities` с уникальным `id`
- [ ] Добавил минимум 1 олимпиаду в `olympiads`
- [ ] Добавил онлайн-курсы в `onlineCourses` (если есть)
- [ ] Добавил очные курсы в `offlineCourses` (если есть)
- [ ] Добавил профильные классы в `profileClasses` (если есть)
- [ ] Добавил фестивали в `festivals` (если есть)
- [ ] Во всех ресурсах указал `relevantUniversities: ["новый_id"]`
- [ ] Проверил JSON на ошибки через jsonlint.com
- [ ] Сохранил файл и протестировал на сайте

